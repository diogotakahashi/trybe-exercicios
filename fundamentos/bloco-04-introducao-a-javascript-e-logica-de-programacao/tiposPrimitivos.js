let patientId = "50";
let isEnrolled = true;
const patientInfo = {
  firstName: 'Ana',
  lastName: 'Santos',
};
const patientEmail = 'ana@email.com';

console.log (typeof patientId, typeof isEnrolled,
     typeof patientInfo, typeof patientEmail)

// operações simples para encontrarmos a área e o perímetro de um retângulo de base 5 e altura 8.

const base = 5
var height = 8
const area = base * height
const perimeter = (base * 2) + (height * 2)

console.log (area, perimeter)